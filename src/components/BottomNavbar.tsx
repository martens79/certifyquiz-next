// src/components/BottomNavbar.tsx (o percorso equivalente)
'use client';

/**
 * BottomNavbar legacy (Vite) — disabilitata in versione Next.js.
 * Tenuta come stub per non rompere gli import esistenti.
 */
export default function BottomNavbar() {
  return null;
}
